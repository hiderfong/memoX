[pytest]
pythonpath = .
testpaths = tests
markers =
    e2e: end-to-end tests requiring real API keys (deselect with '-m "not e2e"')
    browser_e2e: browser UI end-to-end tests requiring local dev servers and an installed Playwright browser
addopts = -q --tb=short
